#pragma GCC optimize("O3,unroll-loops") #pragma GCC target("avx2,bmi,bmi2,lzcnt,popcnt")
#include <bits/stdc++.h>

#ifdef LOCAL
#define px(x) cerr << #x << ' ' << x << '\n';
#define pv(v) cerr << #v << ": "; for (auto& n: v) cerr << n << " \n"[&n == &v.back()];
#else
#define px(x)
#define pv(v)
#endif

#define sz(v) v.size()
#define all(v) v.begin(), v.end()

#define FOR(i, S, T) for (int i = S, ttt = T; i < ttt; i++)
#define ROF(i, S, T) for (int i = T, sss = S; i >= sss; i--)

using namespace std;

const int mod = 1e9 + 7;
typedef __int128 lll;
typedef long long ll;
typedef pair<int, int> pii;


int main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);

}

